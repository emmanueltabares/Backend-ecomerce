/* describe('prueba usando Jest', () => {
    it('Probando libreria JEST', () => {
        const a = 1;
        const b = 1;

        const result = a + b;

        expect(result).toEqual(2);
    })
}) */